# Copiar a /etc/nginx/conf.d/ (contexto http {}) — INF-305
# Ajuste inicial: 10 req/s por IP en /api con ráfaga 20
limit_req_zone $binary_remote_addr zone=horario_api:10m rate=10r/s;
limit_req_status 429;
