# HorarioPro — Frontend

React + Vite + Tailwind CSS (mobile first).

## Desarrollo local

```bash
cd frontend
npm install
cp .env.example .env
npm run dev
```

App: http://localhost:5173

## Build

```bash
npm run build
npm run preview
```
