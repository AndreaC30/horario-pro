/** Tipografía WorkShift — estilo denso GDH, acento azul. */

/** Labels cortos uppercase (métricas, eyebrows). */
export const TYPE_EYEBROW =
  "font-mono text-[11px] font-medium uppercase tracking-[0.06em] text-text-muted sm:text-xs";

/** Líneas secundarias bajo títulos. */
export const TYPE_CAPTION = "text-sm text-text-secondary sm:text-base normal-case";

/** Párrafos y explicaciones. */
export const TYPE_BODY = "text-sm leading-relaxed text-text-secondary sm:text-base";

/** Labels de formulario. */
export const TYPE_LABEL = "text-sm font-medium text-text-secondary";

/** Hints. */
export const TYPE_HINT = "text-sm leading-relaxed text-text-muted";

/** Meta pequeña. */
export const TYPE_META = "font-mono text-xs text-text-muted sm:text-sm";

/** Números hero / KPI grandes. */
export const TYPE_HERO_NUMBER =
  "font-display text-2xl font-semibold tabular-nums tracking-tight text-primary min-[375px]:text-3xl sm:text-4xl";

/** Títulos de sección. */
export const TYPE_DISPLAY =
  "font-display text-xl font-semibold tracking-tight text-text-primary sm:text-2xl";

/** Título de card (eyebrow). */
export const TYPE_CARD_TITLE =
  "mb-3 font-mono text-[0.6875rem] font-semibold uppercase tracking-wider text-text-muted";
